/// User-visible app branding. Dart package name stays `money_tracker_app`.
class AppBranding {
  AppBranding._();

  static const displayName = 'Finora';

  /// Android applicationId / iOS bundle id (Play Store & App Store).
  static const applicationId = 'com.finora.app';

  static const version = '1.0.0';

  static const primaryColorHex = '#00B2E7';
}
